name: pull request comment
on:
  issue_comment:                                     
    types: [created, edited, deleted]
jobs:
 aeolus-bot:
   runs-on: [re-centos7]
   steps:
    - name: parse
      run: |
       echo ${{ github.event.comment.body }}
